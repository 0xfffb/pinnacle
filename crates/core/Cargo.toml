[package]
name = "pinnacle-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Core domain and shared logic for Pinnacle"

[dependencies]
async-trait = "0.1"
tower = { version = "0.5", features = ["util"] }
